# NLP-Based-Sentiment-Analyzer
An NLP-based sentiment analyzer built using the IMDb Movie Reviews dataset. It preprocesses text by removing URLs, punctuation, and stopwords, applies stemming and vectorization, and uses a machine learning model to classify reviews as positive or negative.
